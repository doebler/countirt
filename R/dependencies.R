# just for me to remember the dependencies

# library(Rcpp)
# library(fastGHQuad)
# library(RcppGSL)
# library(RcppEigen) # i don't think i need eigen, try without
# library(rlist) # only needed for the old em
# library(rootSolve) # don't know if i need rootsolve anymore, maybe for standard errors,
# # maybe try if i still need it
# library(nleqslv)
